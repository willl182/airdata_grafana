# Session State: airdata_grafana

**Last Updated**: 2026-05-09 21:37 -0500

## Session Objective

Evolve the Grafana downloader V2 and prepare it for local webapp/backend, Docker deployment, and VPS deployment over Tailscale.

## Current State

- [x] Manual validation against Grafana Inspect succeeded in a previous session.
- [x] V2 product decisions remain active:
  - first deployment access: Tailscale
  - app port: `3001`
  - no public domain for initial test
  - allowed Grafana domain: `grafana.canair.io`
  - max initial job range: 10 days
  - default chunk size: 1 day
  - primary output: CSV long
  - secondary output: CSV wide optional
  - raw JSON: internal technical artifact
  - ZIP: optional technical/debug artifact
- [x] VPS viability was confirmed previously:
  - Docker 29.4.1
  - Docker Compose v5.1.3
  - 7.8 GiB RAM total, 6.6 GiB available
  - 73 GB free disk
  - OpenClaw running at `127.0.0.1:57086`
- [x] A2 job engine exists:
  - job loading and normalization
  - `chunkSize` parsing
  - chunk generation from `startDate`/`endDate`
  - resumable execution that skips existing raw JSON files
  - per-job `job.json`, `chunks.jsonl`, and `manifest.jsonl`
- [x] The local engine was migrated from JavaScript to TypeScript:
  - `src/grafana/*.js` replaced by `src/grafana/*.ts`
  - shared types added in `src/grafana/types.ts`
  - `tsconfig.json` added
  - `typescript` and `@types/node` added as dev dependencies
  - wrappers in `scripts/*.js` preserved
  - public commands now build and execute from `dist/`
  - `dist/` added to `.gitignore`
- [x] Plans updated and aligned:
  - `plan_maestro_v2.md`
  - `plan_v2.md`
  - `subplan_agente_v2.md`
  - `subplan_usuario_v2.md`
  - `plan_migracion.md`
- [x] Verification passed:
  - `pnpm run typecheck`
  - `pnpm run build`
- [ ] Current TypeScript migration + plan updates are ready for final review, commit, and push.
- [ ] A3 implementation is the next functional phase.

## Critical Technical Context

- Working directory: `/home/w182/w421/grafana`.
- Branch: `main`.
- Project remains CommonJS at package level: `"type": "commonjs"`.
- Source of truth for the engine is now TypeScript in `src/grafana/*.ts`.
- `dist/` is generated by `pnpm run build` and is ignored by git.
- Existing commands must remain valid:
  - `pnpm run explore`
  - `pnpm run download`
  - `pnpm run csv`
  - `pnpm run download:job -- examples/job.example.json`
  - `pnpm run download:job -- examples/job.7d.example.json`
- Commands now compile before running CLI wrappers from `dist/scripts`.
- Playwright real downloads may need to run outside the assistant sandbox. In this environment, sandboxed Chromium previously failed with `sandbox_host_linux.cc:41 shutdown: Operation not permitted`; running approved `pnpm run download:job` outside the sandbox solved it.
- Do not expose the app publicly for the first VPS deployment. Use Tailscale and port `3001`.
- Keep user-facing normal download simple: one CSV long final, optional CSV wide. JSON and ZIP are technical artifacts.
- `plan_migracion.md` records the TypeScript migration; phases 1-4 are complete, A3 typed outputs remain pending.

## Next Steps

1. Review the TypeScript migration diff.
2. Commit and push the TypeScript migration plus synchronized plans.
3. Start A3: generate final job-level CSV long and optional wide output, with dedupe for overlapping chunks.
4. Register final artifacts per job for the future API/webapp.
